'use client';import {useState} from 'react';export function ProjectForm(){const[msg,setMsg]=useState('');async function go(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const title=String(f.get('title'));const summary=String(f.get('summary'));const body={slug:f.get('slug'),category:f.get('category'),status:'DRAFT',featured:false,sortOrder:0,title:{en:title,de:title,uk:title,ru:title,sk:title,fr:title},summary:{en:summary,de:summary,uk:summary,ru:summary,sk:summary,fr:summary},websiteUrl:'',githubUrl:'',coverImage:'',technologies:[]};const r=await fetch('/api/admin/projects',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(body)});setMsg(r.ok?'Project added. Refresh to see it.':'Could not add project.')}return <form className="panel form" onSubmit={go}><h2>Add empty project case</h2><input name="slug" placeholder="project-slug" required/><input name="category" placeholder="Category" required/><input name="title" placeholder="Title" required/><textarea name="summary" placeholder="Short summary" required/><button>Add draft</button><span>{msg}</span></form>}