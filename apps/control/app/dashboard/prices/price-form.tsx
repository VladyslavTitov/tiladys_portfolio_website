'use client';import {useState} from 'react';export function PriceForm({sections}:{sections:any[]}){const[msg,setMsg]=useState('');async function go(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const n=String(f.get('name'));const note=String(f.get('note'));const body={sectionId:f.get('sectionId'),code:f.get('code'),name:{ru:n,en:n,de:n,uk:n,sk:n,fr:n},price:f.get('price'),note:{ru:note,en:note,de:note,uk:note,sk:note,fr:note},sortOrder:999,active:true};const r=await fetch('/api/admin/prices',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(body)});setMsg(r.ok?'Price line added. Refresh to see it.':'Could not add line.')}return <form className="panel form" onSubmit={go}><h2>Add price line</h2><select name="sectionId">{sections.map(s=><option key={s.id} value={s.id}>{s.number} {s.title}</option>)}</select><input name="code" placeholder="Code" required/><input name="name" placeholder="Service name" required/><input name="price" placeholder="Price" required/><textarea name="note" placeholder="Note"/><button>Add line</button><span>{msg}</span></form>}