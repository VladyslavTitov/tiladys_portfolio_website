'use client';import{useState}from'react';export function ReplyForm({id,email}:{id:string,email:string}){const[msg,setMsg]=useState('');async function go(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const reply=String(new FormData(e.currentTarget).get('reply'));const r=await fetch(`/api/admin/messages/${id}/reply`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({reply})});setMsg(r.ok?'Reply sent.':'Reply failed. Check SMTP.')}return <form className="reply" onSubmit={go}><textarea name="reply" required placeholder={`Reply to ${email}`}/><button>Send reply</button><span>{msg}</span></form>}