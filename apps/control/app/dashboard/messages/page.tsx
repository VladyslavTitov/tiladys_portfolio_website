import {db} from '@tiladys/db';import {ReplyForm} from './reply-form';export default async function M(){const rows=await db.contactMessage.findMany({orderBy:{createdAt:'desc'},take:100});return <><h1>Customer messages</h1>{rows.map(m=><article className="panel message" key={m.id}><header><b>{m.name}</b><span>{m.email}</span><em>{m.status}</em></header><p>{m.message}</p><small>{m.createdAt.toLocaleString()}</small><ReplyForm id={m.id} email={m.email}/></article>)}</>}