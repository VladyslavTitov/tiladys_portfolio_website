import nodemailer from 'nodemailer';function transport(){if(!process.env.SMTP_HOST)return null;return nodemailer.createTransport({host:process.env.SMTP_HOST,port:Number(process.env.SMTP_PORT??587),secure:Number(process.env.SMTP_PORT)===465,auth:process.env.SMTP_USER?{user:process.env.SMTP_USER,pass:process.env.SMTP_PASSWORD}:undefined})}export async function sendMail(to:string,subject:string,text:string){const t=transport();if(!t){console.warn('SMTP not configured:',subject);return}await t.sendMail({from:process.env.SMTP_FROM,to,subject,text})}
