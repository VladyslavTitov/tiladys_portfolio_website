import type { MetadataRoute } from 'next'; import {locales} from '@tiladys/shared'; export default function sitemap():MetadataRoute.Sitemap{const base=process.env.NEXT_PUBLIC_SITE_URL??'https://tiladys.com'; const routes=['','portfolio','prices','about','contact']; return locales.flatMap(l=>routes.map(r=>({url:`${base}/${l}${r?'/'+r:''}`,lastModified:new Date(),changeFrequency:r==='portfolio'?'weekly':'monthly',priority:r===''?1:.8}))) }